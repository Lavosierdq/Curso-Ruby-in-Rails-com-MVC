<h1>Articles</h1>

<!--
  <ul>
  <% @articles.each do |article|%>
    <li>
    <%= link_to article.title,"/articles/#{article.id}" %> 
    <%= link_to article.title,article %>
    </li>
  <% end %>
  </ul>
-->

<%= render 'longer_featured' %>
<!-- div da linha 226 -->
<div class="row mb-2">
  <%= render 'medium_featured' %>
  <%= render 'medium_featured' %>
</div>

<div class="row g-5">
  <div class="col-md-8">
    <h3 class="pb-4 mb-4 fst-italic border-bottom">
      From the Firehose
    </h3>
    <p><%= link_to 'New_article',new_article_path,class:'btn btn-primary' %></p>
    <%= render @articles %>
    <%= render 'pagination' %>
  </div>
  <%= render 'rigth_side' %>
</div>
